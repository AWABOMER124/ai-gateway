"""
Tool Registry — runtime-validated tool definitions for the AI Core platform.

Every tool the AI can invoke must be registered here with a risk level,
required permissions, and product scope. Un-registered tool names are
rejected outright.
"""
